### **프로젝트 현황 분석 및 실행 계획 (최종)

#### **1. 최종 결론: 무엇이 되어 있고, 무엇이 남았는가?**

**"뼈대는 훌륭하게 완성되었고, 이제 살을 붙일 차례입니다."**

프로젝트는 현재 Next.js와 shadcn/ui를 기반으로 한 확장성 있는 구조를 갖추고 있습니다. 특히, 데이터 로직을 `useDashboard` 훅으로 분리한 것은 매우 좋은 설계입니다.

*   **이미 구현된 기능 (DB 연동 완료):**
    *   **기관 정보 표시:** 대시보드 상단의 기관 이름.
    *   **KPI 숫자 값:** 총 사용자, 오늘/주간 활성 사용자 등 6개 KPI의 현재 값.
    *   **테이블 데이터:** '최근 활동 사용자'와 '비활성 사용자' 목록.

*   **앞으로 구현할 기능 (현재 하드코딩 상태):**
    1.  **KPI 증감률:** KPI 카드에 표시되는 `+2.1%` 와 같은 이전 기간 대비 변경률.
    2.  **차트 데이터:** 아래 3개의 차트는 현재 모두 정적인 더미 데이터로 그려지고 있습니다.
        *   성별 분포 (Gender Distribution)
        *   일일 활동 (Daily Activity)
        *   건강 상태 분포 (Health Status Distribution)

#### **2. 원인 분석: 왜 일부만 연동되었는가?**

`useDashboard` 훅을 분석한 결과, 원인은 명확합니다.

*   **KPI 증감률:** 현재 훅은 '이번 주' 데이터만 가져옵니다. 증감률을 계산하려면 **'지난주' 데이터**를 함께 가져와 비교하는 로직이 추가되어야 합니다.
*   **차트 데이터:** 현재 훅은 차트에 필요한 데이터(예: 시니어의 성별 통계, 일별 접속 현황 등)를 **전혀 조회하거나 계산하지 않습니다.**

#### **3. 실행 계획: 3단계로 완성하는 동적 대시보드**

이제 명확한 목표를 가지고, 아래 3단계에 따라 대시보드를 100% 동적으로 만들겠습니다.

**1단계: Supabase 데이터베이스 함수(RPC) 설계 및 생성**

복잡한 데이터 계산은 프론트엔드가 아닌 Supabase 백엔드에서 처리하는 것이 효율적입니다. 아래 3개의 데이터베이스 함수(RPC)를 만들어, 필요한 모든 데이터를 한 번의 호출로 가져오도록 설계하겠습니다.

1.  **`get_kpi_summary(org_id)`**: KPI 숫자 값과 **증감률 계산에 필요한 이전 기간 데이터**까지 모두 계산하여 반환하는 함수.
2.  **`get_chart_data(org_id)`**: 성별, 일일 활동, 건강 상태 등 **3개 차트에 필요한 모든 데이터를** 계산하여 반환하는 함수.
3.  **`get_user_activity_tables(org_id)`**: '최근 활동' 및 '비활성' 사용자 테이블 데이터를 계산하여 반환하는 함수. (현재 `useDashboard` 훅의 로직을 이전)

**2단계: `useDashboard` 훅 리팩토링**

기존의 복잡한 데이터 조회 및 계산 로직을 모두 제거하고, 위에서 만든 3개의 RPC 함수를 호출하는 간결한 코드로 리팩토링합니다.

*   `useDashboard` 훅은 이제 3개의 RPC를 호출하고, 그 결과를 state에 담아 UI 컴포넌트로 전달하는 역할만 맡게 됩니다.

**3단계: UI 컴포넌트 데이터 연결**

*   리팩토링된 `useDashboard` 훅이 반환하는 실제 DB 데이터를 각 KPI 카드와 차트 컴포넌트에 연결합니다.
*   하드코딩된 더미 데이터 배열을 모두 삭제하고, 훅에서 내려주는 동적 데이터로 교체합니다.
