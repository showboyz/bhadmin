# 대시보드 실제 데이터 연동 계획서 (상세 버전)

## 1. 프로젝트 목표
현재 더미 데이터(dummy data)로 구현된 대시보드를 실제 Supabase 데이터베이스와 연동하여, 실시간 데이터를 기반으로 동작하는 동적인 대시보드를 구현한다.

## 2. 현재 상태 및 문제점

- **데이터 비연동:** 대시보드 UI가 실제 데이터베이스와 연결되어 있지 않아 정적인 화면만 보여줌.
- **데이터 부재:** 테스트에 필요한 실제 데이터가 데이터베이스에 없음.
- **API 부재:** 프론트엔드와 백엔드를 연결하는 데이터 통신 계층(API)이 없음.

## 3. 페이지별 상세 기능 정의 및 설계

### 3.1. 대시보드 페이지 (`/dashboard`)
메인 대시보드는 전체 현황을 한눈에 파악할 수 있는 요약 정보를 제공합니다.

- **기능 1: 요약 통계 카드**
  - **내용:** 총 시니어 수, 오늘 예정된 스케줄 수, 오늘 완료된 검사 수, 주의가 필요한 시니어 수.
  - **데이터:** `seniors` 테이블 count, `schedules` 테이블 count (조건: 오늘 날짜, 상태별), `cognitive_results` 또는 `motor_results` 테이블 count (조건: high risk).
  - **API 설계:**
    - `GET /api/dashboard/stats`: 위의 4가지 통계 데이터를 한 번에 조회하는 API.

- **기능 2: 스케줄 현황 차트**
  - **내용:** 최근 7일간의 '예정된 스케줄'과 '완료된 스케줄' 수를 비교하는 바(Bar) 차트.
  - **데이터:** 지난 7일간의 `schedules` 데이터를 날짜별, 상태별로 그룹화한 통계.
  - **API 설계:**
    - `GET /api/dashboard/charts/schedules?range=7d`: 차트에 필요한 날짜별 스케줄 데이터를 제공.

- **기능 3: 최근 활동 목록**
  - **내용:** 최근 5개의 주요 활동(예: '홍길동 시니어 신규 등록', '김영희 시니어 검사 완료')을 보여주는 목록.
  - **데이터:** `audit_log`와 같은 별도 테이블 또는 여러 테이블(seniors, schedules)의 생성/수정 이력을 조합.
  - **API 설계:**
    - `GET /api/dashboard/recent-activity?limit=5`: 최근 활동 목록 데이터를 제공.

### 3.2. 시니어 관리 페이지 (`/seniors`)
기관에 등록된 시니어 정보를 관리합니다.

- **기능 1: 시니어 목록 조회**
  - **내용:** 검색 및 정렬 기능이 포함된 시니어 목록 테이블 (컬럼: 이름, 나이, 담당자, 최근 검사일, 상태).
  - **데이터:** `seniors` 테이블을 기준으로 `users`(담당자), `schedules`(최근 검사일) 테이블과 JOIN된 데이터.
  - **API 설계:**
    - `GET /api/seniors?search=...&sort=...`: 시니어 목록 데이터를 페이지네이션과 함께 제공.

- **기능 2: 시니어 상세 정보 조회 (`/seniors/[id]`)
  - **내용:** 특정 시니어의 기본 정보, 모든 스케줄 내역, 과거 검사 결과 요약 등을 보여주는 페이지.
  - **API 설계:**
    - `GET /api/seniors/[id]`: 특정 시니어의 모든 관련 정보를 제공.

### 3.3. 스케줄 관리 페이지 (`/schedules`)
검사 스케줄을 관리합니다.

- **기능 1: 캘린더 뷰**
  - **내용:** 월/주/일 단위로 스케줄을 보여주는 캘린더. 스케줄 상태(예정, 완료, 취소)에 따라 색상 구분.
  - **데이터:** 특정 기간 내의 모든 `schedules` 데이터.
  - **API 설계:**
    - `GET /api/schedules?start_date=...&end_date=...`: 캘린더에 표시할 스케줄 데이터를 제공.

- **기능 2: 스케줄 생성/수정**
  - **내용:** 특정 시니어에게 새로운 검사 스케줄을 할당하거나 기존 스케줄을 변경하는 기능.
  - **API 설계:**
    - `POST /api/schedules`: 새 스케줄 생성.
    - `PUT /api/schedules/[id]`: 기존 스케줄 수정.

## 4. 단계별 실행 계획

1.  **[1단계] 데이터베이스 준비:**
    - **(수행 필요)** `supabase.ts` 스키마를 기반으로 `organisations`, `users`, `seniors`, `schedules` 테이블에 테스트용 더미 데이터를 생성하는 SQL 스크립트를 작성하고 실행한다.

2.  **[2단계] API 엔드포인트 구현 (Next.js API Routes):**
    - **(수행 필요)** `GET /api/dashboard/stats` API를 가장 먼저 구현한다.

3.  **[3단계] UI 연동 (점진적):**
    - **(수행 필요)** 대시보드 페이지의 요약 통계 카드부터 실제 API 데이터와 연동한다.

4.  **[4단계] 전체 기능 확장 및 테스트:**
    - **(수행 필요)** 나머지 페이지들의 API와 UI를 순차적으로 연동하고, Playwright로 기능 테스트를 자동화한다.


## 1. 프로젝트 목표
현재 더미 데이터(dummy data)로 구현된 대시보드를 실제 Supabase 데이터베이스와 연동하여, 실시간 데이터를 기반으로 동작하는 동적인 대시보드를 구현한다.

## 2. 현재 상태 및 문제점

### 현재 상태
- **인증 시스템:** Supabase를 이용한 사용자 로그인 및 인증 기능은 구현되어 있음.
- **기본 UI:** 대시보드의 전체적인 레이아웃과 컴포넌트(카드, 차트 등)는 하드코딩된 더미 데이터로 구현되어 있음.
- **데이터베이스:** Supabase에 `organisations`, `seniors`, `schedules` 등 관련 테이블 스키마는 존재함. (`add-schedules-data.sql` 파일 확인)

### 문제점
- **데이터 비연동:** 대시보드 UI가 실제 데이터베이스와 연결되어 있지 않아, 정적인 화면만 보여줌.
- **데이터 부재:** 대시보드에 필요한 데이터를 표시하기 위한 실제 데이터가 데이터베이스에 부족하거나 없음. (`add-schedules-data.sql` 스크립트는 이 문제를 해결하기 위한 시도로 보임)
- **API 부재:** 프론트엔드(대시보드)와 백엔드(Supabase)를 연결해 줄 데이터 통신 계층(API)이 구현되어 있지 않음.

## 3. 해결 방안 및 설계

### 3.1. 데이터베이스 설계 (Backend - Supabase)
- **데이터 확인 및 추가:** 대시보드 기능에 필요한 모든 데이터가 충분히 있는지 확인. `add-schedules-data.sql` 와 같은 스크립트를 검토하고 실행하여 테스트에 필요한 데이터를 데이터베이스에 채워 넣는다.
- **데이터베이스 함수 (RPC):** 복잡한 데이터 조회(예: 통계 계산)는 Supabase의 데이터베이스 함수(RPC)로 만들어, 프론트엔드에서는 간단하게 함수만 호출하도록 설계한다. 이렇게 하면 프론트엔드 코드가 깔끔해지고 성능도 개선된다.

### 3.2. API 계층 설계 (Middleware)
- **Next.js API Routes 활용:** Next.js의 API 라우트 (`src/app/api/...`)를 사용하여 데이터베이스와 통신하는 API 엔드포인트를 만든다.
- **주요 API 엔드포인트 예시:**
  - `GET /api/dashboard/stats`: 대시보드 상단의 주요 통계(총 회원 수, 활성 스케줄 수 등) 데이터를 제공.
  - `GET /api/dashboard/recent-activity`: 최근 활동 내역을 제공.
  - `GET /api/dashboard/schedule-overview`: 스케줄 현황 데이터를 차트에 맞게 가공하여 제공.

### 3.3. UI 계층 설계 (Frontend - Next.js)
- **데이터 Fetching 로직 추가:** 대시보드 페이지 컴포넌트에서 위에서 설계한 API를 호출하여 데이터를 가져오는 로직을 추가한다. (React-Query 또는 SWR 라이브러리 사용을 권장)
- **컴포넌트 리팩토링:** 기존에 더미 데이터를 사용하던 각 UI 컴포넌트(카드, 차트 등)를 API로부터 받은 실제 데이터를 `props`로 받아 표시하도록 수정한다.
- **상태 관리:** 데이터 로딩 중, 에러 발생 시의 UI 상태(로딩 스피너, 에러 메시지 등)를 구현하여 사용자 경험을 개선한다.

## 4. 단계별 실행 계획

1.  **[1단계] 데이터베이스 준비:**
    - `add-schedules-data.sql` 스크립트를 실행하여 데이터베이스에 스케줄 데이터를 추가한다.
    - 대시보드 통계에 필요한 다른 데이터(예: 시니어, 기관)도 확인하고 필요시 추가한다.

2.  **[2단계] API 엔드포인트 구현:**
    - 대시보드 통계 데이터를 조회하는 `GET /api/dashboard/stats` API를 먼저 구현한다.

3.  **[3단계] UI 연동 (점진적):**
    - 대시보드 메인 컴포넌트에서 `stats` API를 호출하도록 수정한다.
    - 가장 간단한 통계 카드 UI부터 더미 데이터를 실제 데이터로 교체한다.

4.  **[4단계] 전체 기능 확장:**
    - 차트, 최근 활동 등 나머지 대시보드 컴포넌트들에 필요한 API를 추가로 구현하고 UI를 연동한다.

5.  **[5단계] 테스트 및 검증:**
    - Playwright를 사용하여 로그인 후 대시보드에 실제 데이터가 잘 표시되는지 확인하는 자동화 테스트 코드를 작성한다.

---
